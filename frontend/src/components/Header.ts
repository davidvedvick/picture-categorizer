import styled from "styled-components";

export const Header = styled.header`
    max-height: 64px;

    img {
        max-height: 48px;
        padding-left: 8px;
        padding-right: 8px;
    }
`;
