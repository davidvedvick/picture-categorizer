ktor {
    deployment {
        port = 5000

        // For dev purpose
        //autoreload = true
        //watch = [org.koin.sample]
    }

    application {
#         modules = [ org.koin.sample.Application.main ]
    }
}

datasource {
    url = "jdbc:mariadb://localhost:3306/catpics"
    user = "cat"
    password = "scratch"
}

authentication {
    secret {

    }
}